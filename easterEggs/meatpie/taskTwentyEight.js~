const inputCollector = require("prompt-sync")();

const firstNum = Number(inputCollector("What's your first number? "))
const secondNum = Number(inputCollector("What's your second number? "))
    
const sum = firstNum + secondNum
const product = firstNum * secondNum
console.log(`your sum is ${sum} while your product is ${product}`)
