const prompt = require("prompt-sync")();
const sub_total = Number(prompt("Enter the subtotal: "));
const rate = Number(prompt("Enter the gratuity rate: "));

console.log(gratuity = sub_total * (rate/100))
console.log(total = sub_total + gratuity)
